# DROS In-Band Execution Guardrail Rules for Cursor
# Ensure all terminal and file modification tools undergo pre-execution evaluation via DROS Gateway (http://localhost:8080)
# Before running any destructive commands (rm, wipe, git reset --hard, curl to unknown IPs), verify policy compliance.

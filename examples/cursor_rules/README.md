# 💻 Cursor IDE Agent Governance

Protect your local development environment from aggressive Cursor terminal execution or accidental workspace wiping.

## Quick Setup
1. Copy `.cursorrules` into your project root directory.
2. In your Cursor terminal settings or task scripts, route tool checks through `http://localhost:8080/evaluate`.

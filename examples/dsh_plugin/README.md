# 📦 DeepSeek Harness (DSH) Dual-Mode Plugin

Integrate DSH directly with DROS Hacker Gateway for unified multi-agent governance.

## Setup
```bash
dsh plugin --profile web add dsh-plugin-vajraclaw
```
Set `gatewayUrl` in DSH settings to `http://localhost:8080`.
